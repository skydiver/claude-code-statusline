# claude-code-statusline — basic template
#
# Single-line layout matching the shell script's BASIC template.
# Copy this file to ~/.config/claude-code-statusline/config.toml
# (or $XDG_CONFIG_HOME/claude-code-statusline/config.toml).

format = "🤖 $model | 💰 $cost | 📈 Session: $session [$session_reset] | 📅 Weekly: $weekly [$weekly_reset] | 🧠 $context_bar$git_branch_sep"
