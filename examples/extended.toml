# claude-code-statusline — extended template
#
# Two-line layout matching the shell script's EXTENDED template.
# Uses a TOML multi-line basic string for readability. The literal
# newline between the two lines is preserved in the rendered output.

format = """
🤖 $model | 💰 $cost | ⏱️ $duration | 📈 $session [$session_reset] | 📅 $weekly [$weekly_reset] | 🧠 Context: $context
🚀 Claude Code $version | ⬇️ Tokens In: $tokens_in | ⬆️ Tokens Out: $tokens_out | ♻️ Cache: $cache ( | 🌿 $git_branch ) | 📁 $project\
"""
